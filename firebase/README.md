# Firebase Setup

1. Create a Firebase project.
2. Enable Authentication (Email/Password) and Firestore.
3. Add a Web App to get client keys, then fill `ENV_EXAMPLE.txt` values into `.env.local`.
4. For hosting, install Firebase CLI and run `firebase init hosting` (do not commit secrets).


